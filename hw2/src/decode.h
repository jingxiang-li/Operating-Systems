#ifndef DECODE_H
#define DECODE_H

#include <stdio.h>

/**
 * decode a given file
 * @param  filepath path to the file
 * @return          0 on success, otherwise -1.
 */
int decode_file(char *filepath);

#endif  // DECODE_H
